#!/bin/bash
# Скрипт для запуска локального сервера (Rust/Axum)

echo "=========================================="
echo "🚀 Запуск локального сервера (Axum)..."
echo "=========================================="

# Переходим в директорию сервера
cd "$(dirname "$0")/src-tauri/server" || exit

# Запускаем сервер
cargo run
