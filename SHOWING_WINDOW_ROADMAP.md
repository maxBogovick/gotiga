# «Дом просыпается» — окно показа: дорожная карта и прогресс

> Живой документ. Это и спецификация, и трекер. Веду я (Claude) по ходу разработки.
> **Протокол:** перед реализацией КАЖДОЙ задачи — отдельно проговариваю с владельцем,
> что именно буду делать (модель данных, файлы, поведение), получаю «да», только потом
> пишу код. Статус и заметки обновляю здесь же, чтобы ничего не потерять между сессиями.
> Дата старта: 2026-06-23.

## Легенда статусов
- `[ ]` не начато · `[~]` обсуждается / в работе · `[x]` готово и проверено
- Каждая задача: **Цель → Модель данных → Файлы → Переиспользовать → Приёмка → Зависит от**

## J. Доска объявлений дома + страница зала — `[x]` (2026-06-24)
> **Реализовано.** Чистые helper'ы в `showing-window.ts`: `nextOpening(w, now, 7д)` (шагает
> по дням, подтверждает реальное открытие через `isShowingOpen` на +1 мин — учитывает часы/дни/дату
> и обёртку через полночь), `describeOpening` → `{at, tone, dayOffset, weekday}` (tone:
> today/tonight/tomorrow/tomorrowNight/weekday/date), `openingWhenLabel` (атмосферная относительная
> фраза: i18n для ближних дней, Intl-имя дня для 2–6 дней, Intl-дата дальше — без отсчёта),
> `roomToWindow` (вынесен из admin, дедуп). Компонент `components/HouseNoticeBoard.svelte`: дубовая
> доска, пергаментные листки на кованых гвоздях (rotate, foxing), до 3 залов с открытием в горизонте
> 7 дней; состояния спящий → glow по приближении (доля от `minutesUntilOpen`, reuse F) → «открыто ·
> войти» (тёплый медный воск); прячется целиком, когда пусто; врезан в `routes/+page.svelte` сразу
> после героя (`figurines={collectionFigurines}`). Страница зала `routes/hall/[id]/` (+loader с
> entries для web-prerender, как у figurines/[id]; залы из `getShowingRooms`, работы фильтром по
> `showingRoomId`): шапка с именем зала, расписанием (открыто / «откроется когда»), нотой
> запечатано/открыто, грид `HomeFigurineTile` (гейт уже внутри — двери заперты до открытия,
> раскрываются в окне live). i18n en+ru (`board*`, `hall*`). `noindex` на зале. Тики от `house-clock`.
> admin рефакторнут на общий `roomToWindow`. `npm run check` — мои файлы зелёные (2 ошибки в
> analytics-панелях — несвязанная WIP). Отложено: индекс `/halls`, «все показы» при >3, `showing_rooms.note`.
> **Расширение (2026-06-24):** доска вешает листки не только для залов, но и для одиночных
> гейтованных работ (свои часы, без зала) — листок ведёт на `/figurines/[id]`. Два источника
> (залы → `/hall/[id]`, работы → `/figurines/[id]`) сливаются, сортируются (открытые → ближайшие),
> топ-3. Заголовок доски нейтрализован: «Скоро проснётся» / «Waking soon».
> **Редизайн (2026-06-24, опрос):** первая версия (растянутые пергаментные листки на пробковой
> доске) забракована владельцем — пустоты, длинные имена работ как заголовки, плоский картон,
> нарушение letterpress-закона сайта. Выбран концепт «Порог дома»: резная дубовая балка-лінтель,
> каждая запись — гравированная строка-леджер: мотив (❧ работа / ❦ зал) · имя (обрез ellipsis) ·
> выводные точки · атмосферное «когда» · →. Восковая печать при «открыто», тёплое свечение по
> приближении часа (reuse F), латунные уголки, инцизные грувы между строк. Материалы = дверь
> (дуб+латунь+воск+пергаментные чернила). Копирайт «когда» сделан атмосферным («при свете» /
> «когда стемнеет»), убрано календарное «сегодня». Мобайл: имя в 2 строки, леджер скрыт. № работы
> на строке не показываем (нет стабильного каталожного индекса в списке — мотив ❧/❦ различает тип).
> **Редизайн 2 → «Афишная стена» (2026-06-24, опрос):** владелец уточнил — это МУЗЕЙ, не дом;
> нужны «афиши» показов, крутые но атмосферные, не выбиваясь из letterpress-стиля (см. память
> `museum-not-house-framing`). Лінтель/дом/окна забракованы как чужая метафора. Реализовано:
> `HouseNoticeBoard` = стена расклеенных летрпресс-афиш. Ближайший показ — крупная featured-афиша,
> остальные — меньшие билли внахлёст (rotate, foxing, dog-eared угол, paste-shadow). Афиша: эйброу
> СКОРО/ОТКРЫТО (❧ работа/❦ зал) + волосяная линейка · крупное имя (Cormorant, clamp 2–3 стр) ·
> печатная двойная линейка · дата-заголовок («когда») · ВОЙТИ →. Состояния: спит → ламповый прогрев
> по приближении (`--glow` soft-light, reuse F) → красный диагональный надпечаток «ОТКРЫТО» (штамп =
> состояние, не отсчёт). Чистые токены сайта (линейки/воск/Cormorant/чернила) → стиль не ломается.
> Копирайт музейный: эйброу «Афиша»/«Programme», заголовок «Скоро открывается»/«Opening soon»,
> i18n `posterSoon/posterOnView/posterStampOpen`. Мобайл: билли в колонку. reduced-motion учтён.
> `npm run check` — мой файл зелёный.
> **Редизайн 3 → «Вуаль-плакат» (2026-06-24, опрос + само-критика):** landscape-карточки v2 признаны
> провалом (не плакат, пустоты, нет визуала). Само-критика «фото работы на афише»: спойлит спрятанную
> работу (против сути гейта) + пахнет магазином → отвергнуто. Выбран **портретный вуаль-плакат**:
> аспект 4:5 keyhole-панель с лицом работы под `KeyholeVeil` (СОЛ → в тени + восковая печать,
> теплеет лампой по приближении через `--glow` screen; ОТКРЫТО → вуаль спадает, работа проступает +
> штамп «ОТКРЫТО»). Низ — летрпресс: эйброу СКОРО/ОТКРЫТО (❧/❦), крупное имя (clamp 2 стр), двойная
> линейка, дата-заголовок, ВОЙТИ. Зал берёт лицо репрезентативной работы (с картинкой). Переиспользует
> `AppImage`+`KeyholeVeil`+glow+воск. Note расширен полем `art` (face/thumb/focal/darkness). Featured
> шире, билли в ряд → на узком 2/row → 1/row. Зазор любопытства цел, гейт не спойлит. `npm run check`
> мой файл зелёный (1 ошибка в AnalyticsPanel — несвязанная WIP).
> **Фикс A (размер):** v3 рендерился гигантом (`flex:1 1` grow → пол-экрана каждый). → плакат
> фикс-ширины `flex:0 0 clamp(214…276px)`, не растёт; выровнен слева.
> **Фикс B → full-bleed (КЛЮЧЕВОЙ урок):** даже после размера — «чёрный квадрат + текст снизу» = карточка,
> не афиша. КОРЕНЬ черноты: **работы сняты на готическом чёрном фоне**, а затемняющая `KeyholeVeil`
> давала чёрное-на-чёрном → невидимо. Вуаль, которая *вычитает* свет, для тёмного контента не работает.
> Переделано в НАСТОЯЩИЙ плакат: изображение работы **во весь лист** (full-bleed, aspect 3:4), типографика
> **поверх** по нижнему скриму (имя cream + text-shadow, дата, ВОЙТИ; эйброу+воск/штамп сверху). Вместо
> затемнения — **тёплый луч-свеча** на фокус работы (`--fx/--fy`, screen) «вытаскивает» тёмную работу из
> мрака (light reveal, не dark conceal); тонкая прохладная shade = «ещё не зажжён» (снимается при ОТКРЫТО);
> glow растёт по приближении; картинка brightened (`brightness 1.04→1.12`). `KeyholeVeil` убран из этого
> компонента. Зал берёт лицо репр. работы; нет картинки → гравюрный эмблемный фолбэк. `npm run check`
> мой файл зелёный.

### J (исходная карточка)
> **Цель:** мотивировать прийти в конкретное время — на главной (сразу после героя) дубовая
> «доска объявлений» с пергаментными листками-анонсами ближайших открытий залов; клик по листку →
> страница зала `/hall/[id]` с собранными работами (запечатаны до открытия, раскрываются в окне).
> **Решения (опрос 2026-06-24):** метафора = дубовая доска + пергамент на гвоздях (в языке SealedDoor);
> размещение = секция после героя; раскрытие листка = имя зала + атмосферное «когда» (без таймера,
> работы не раскрываем — зазор любопытства); охват = все залы с открытием в горизонте ~7 дней,
> ближайший/открытый выделен. Маршрут `/hall/[id]`; «все показы» при >3 — отложено.
> **Состояния листка:** спящий → тёплый glow по приближении (reuse F) → «открыт · войти» (воск awake).
> **Модель данных:** бэкенд НЕ нужен — залы из `GET /showing-rooms`, работы зала фильтром по
> `showingRoomId` из существующего списка фигур. Опц. позже: `showing_rooms.note` (интро зала), `/halls` индекс.
> **Файлы:** `showing-window.ts` (`nextOpening(w, now, 7d)` + относительный форматтер «когда`),
> `components/HouseNoticeBoard.svelte`, `routes/hall/[id]/+page.svelte` (+loader), врезка в
> `routes/+page.svelte`, i18n en+ru.
> **Переиспользовать:** `HomeFigurineTile` (весь per-work гейт уже внутри), `resolveWindow`/`isShowingOpen`/
> `minutesUntilOpen`, glow (F), `house-clock`, дубово-восковой язык SealedDoor.
> **Приёмка:** зал с окном «сегодня ночью» → на главной листок «когда стемнеет», тёплый glow к ночи,
> в окне — «войти»; клик → зал, до открытия двери заперты, в окне раскрыты и кликабельны; пусто → доски нет.
> **Этика:** имя+когда (не работы), горизонт 7 дней (не «фоновый шум»), без отсчёта, прячется когда пусто.
> **Зависит от:** A (залы), B (расписание), F (glow) — всё готово.

## Этические инварианты (не нарушать ни в одной задаче)
Окно повторяется (день/неделя/год) и контент НИКОГДА не исчезает безвозвратно · расписание
показывается открыто · **никакого** секундного отсчёта, «успей», счётчика ожидающих,
email-напоминаний по умолчанию · любопытство, не тревога. Если фича начинает «ускорять
восприятие» или пахнуть магазином — она неверна.

---

## ✅ База v1 — СДЕЛАНО в этой сессии (фундамент, на нём всё строится)
- [x] Чистые функции окна: `src/lib/showing-window.ts` — `isGated`, `isWindowOpen` (с
      обёрткой через полночь), `windowKind` (night/day), `formatMinutes`, `minutesOfDay`.
- [x] Часы дома: `src/lib/stores/house-clock.svelte.ts` — тик раз в минуту + ресинк на focus/visibility.
- [x] Компонент `src/lib/components/SealedDoor.svelte` — процедурная резная дубовая дверь
      (CSS) ИЛИ ассет по `doorImageUrl`; табличка с фразой + часами.
- [x] Гейт в UI: `HomeFigurineTile.svelte`, `routes/figurines/+page.svelte`,
      `routes/figurines/[id]/+page.svelte` (полноэкранная дверь + защита deep-link; не
      пишем `gotiga_viewed`/keyhole-печать пока дверь закрыта; подавлен view-transition).
- [x] Данные `open_from_min`/`open_until_min` + опц. `sealed_door_image` сквозь весь стек
      (server: migrations `…03`/`…04`, models, DTO, INSERT/SELECT, services; tauri: schema.sql,
      models, repository, commands; frontend types `api.ts`).
- [x] Админка: time-инпуты «Открывается/Закрывается» + URL картины двери (форма фигурки).
- [x] i18n (en источник + ru): `doorPhraseNight/Day`, `doorSealedHint`, `adminFieldShowing*`,
      `adminFieldSealedDoorImage`.

**Ключевая модель для всех будущих задач:** «эффективное окно» фигурки разрешается по
приоритету → *явное окно фигурки* перекрывает *окно зала* перекрывает *нет окна (всегда открыто)*.
Все расширения (дни недели, дата, режим времени, уровень гейта) должны жить на ОБОИХ уровнях
(зал и фигурка) и резолвиться единой чистой функцией в `showing-window.ts`.

---

## 1. Рычаги управления

### A. Окна как «залы» (именованные группы) — `[x]` (2026-06-23)
> Реализовано: таблица `showing_rooms` (server migration `…05` + tauri schema), полный CRUD
> (server: repo/service/handlers + routes `GET /showing-rooms` публичный, `POST`/`DELETE :id`
> admin; tauri: repo/commands + регистрация в lib.rs; `api.ts`: get/save/deleteShowingRoom).
> Фигурка получила `showing_room_id` (FK ON DELETE SET NULL на сервере; на tauri — ручной
> SET NULL при удалении зала). Резолв — `resolveWindow(fig, rooms)` в `showing-window.ts`
> (зал XOR своё время XOR всегда открыто; осиротевший зал → ungated). Стор
> `stores/showing-rooms.svelte.ts` кэширует залы; гейт в home/archive/detail резолвит через него.
> Админка: в форме фигурки селект «Всегда открыто / Своё время / [залы]» + инлайн-менеджер залов
> (`<details>`). i18n en+ru. Проверено: `cargo check` (server+tauri) и `npm run check` зелёные.
- **Цель:** фигурка ссылается на зал, а не хранит часы; правка зала переезжает всю группу.
- **Модель данных (на обсуждение):** залы как список в runtime design/theme-конфиге
  (`{ id, name, openFromMin, openUntilMin, timeBasis, daysMask, … }`) — НЕ новая таблица,
  использовать существующую design-config архитектуру. Фигурка получает поле
  `showing_room_id TEXT` (NULL = нет зала). Разрешение window: явное окно фигурки →
  окно зала по id → ungated.
- **Файлы:** design-config (backend endpoints + `stores/theme.svelte.ts` + админ-таб design),
  `showing-window.ts` (функция `resolveWindow(fig, rooms)`), `api.ts` types, миграция +
  модели/repo для `showing_room_id` (server+tauri), админ-форма фигурки (выбор зала),
  все три места рендера двери (передавать разрешённое окно, не сырые поля).
- **Переиспользовать:** Design System (см. memory `design-system.md`), ось `series`/зон как образец.
- **Приёмка:** создаю «Ночной зал» 23:00–04:00, привязываю 3 фигурки → все закрыты ночью;
  меняю часы зала → у всех трёх сдвинулось; фигурка с явным окном игнорирует зал.
- **Зависит от:** база v1. Фундамент для B/C/D на уровне зала.

### B. Расписание: дни недели + даты — `[x]` (2026-06-23)
> Реализовано на уровне ЗАЛА (режим «своё время» у фигуры остаётся часами-только; модель
> данных умеет нести дни/дату из любого источника). Поля `showing_rooms`: `open_days_mask`
> (7 бит Пн..Вс; NULL/0/127 = каждый день), `open_month_day` (MM-DD — ежегодно),
> `open_date_from`/`open_date_until` (YYYY-MM-DD — разовый интервал; ежегодно XOR интервал).
> Server migration `…06` + tauri schema; модели/DTO/repo/upsert server+tauri; типы `api.ts`.
> Логика — `isShowingOpen(w, Date)` в `showing-window.ts`: дата → день недели → часы; окно через
> полночь считает день по началу; `isGated` расширен (часы ИЛИ дни ИЛИ дата). `house-clock`
> отдаёт `nowDate` (поминутно) для пересчёта на сменах суток/дат. `SealedDoor` показывает на
> табличке дату/дни/часы (Intl по `$lang`). Админка: во вкладке «Залы показа» — 7 кнопок-дней
> + переключатель даты (нет/ежегодно/интервал) с date-инпутами. i18n en+ru. Все сборки зелёные.
> Известный нюанс: у all-day зала (from==until) фраза двери берётся по windowKind (день/ночь) —
> может звучать неточно; дни/дата на табличке несут реальную инфу. Уточнить копирайт позже.

### B (исходная карточка). Расписание: дни недели + даты — `[x]`
- **Цель:** «открыто по воскресеньям»; «экспонат-годовщина» — одна ночь в год.
- **Модель данных:** `open_days_mask INTEGER` (биты Пн..Вс, NULL/127 = все дни);
  опц. годовое окно `open_month_day_from`/`_until` (MM-DD) и/или абсолютные даты — решить на обсуждении.
  Живёт и на зале, и на фигурке.
- **Файлы:** `showing-window.ts` (`isWindowOpen` принимает `Date`, учитывает день недели/дату),
  миграция + модели/repo/DTO (server+tauri), `api.ts`, админ-форма (чекбоксы дней / выбор даты),
  `house-clock` (отдавать не только минуты, но и день/дату — или передавать `new Date()`).
- **Приёмка:** окно «только Вс 00:00–24:00» закрыто Пн–Сб; годовое окно открыто только в свою дату.
- **Зависит от:** желательно после A (чтобы маска была и на зале). Можно начать с уровня фигурки.

### C. Режим времени `local` | `house` — `[ ]`
- **Цель:** редкий экспонат, который весь мир смотрит в одну общую полночь.
- **Модель данных:** `time_basis TEXT DEFAULT 'local'` на зале/фигурке; IANA-зона дома —
  константа/конфиг (НЕ offset, чтобы DST не ломал полночь). `local` = по часам гостя (как сейчас).
- **Файлы:** `showing-window.ts` (вычислять «сейчас» в зоне дома через `Intl.DateTimeFormat`
  с `timeZone`), `house-clock` (вариант house-now), модели/миграция/DTO, админ-форма (переключатель).
- **Приёмка:** фигурка в режиме house открывается в один и тот же абсолютный момент независимо
  от часов гостя; DST не сдвигает «полночь дома».
- **Зависит от:** A (режим логично хранить на зале).

### D. Уровень гейта — «полудверь» — `[ ]`
- **Цель:** третий режим между «дверь» и «открыто»: днём виден лит-фрагмент через `KeyholeVeil`,
  ночью снимается всё.
- **Модель данных:** `gate_level TEXT DEFAULT 'door'` ∈ `door` | `veil`. При `veil` и закрытом окне
  рендерить `KeyholeVeil` (как сейчас на «непрочитанных»), а не `SealedDoor`; при открытом — полностью.
- **Файлы:** в основном фронт — три места рендера (ветка по `gate_level`), `SealedDoor` не трогаем,
  `KeyholeVeil` переиспользуем; поле в моделях/DTO/админке.
- **Приёмка:** `veil`-фигурка днём показывает скважину (кликабельна? решить — вероятно НЕ входит,
  но дразнит), ночью открывается целиком.
- **Зависит от:** база v1. Независима от A.

### E. Админ-предпросмотр «как будто сейчас X часов» — `[x]` (2026-06-23)
> Фронт-онли, без БД. Общее состояние `previewAt: Date` в admin (datetime-local + «Сейчас»).
> Вкладка «Залы показа»: контрол вверху + у каждого зала бейдж Открыто/Заперто на этот момент
> (`isShowingOpen(roomToWindow(room), previewAt)`). Форма фигуры: бейдж по разрешённому окну
> (`resolveWindow`) + мини-`SealedDoor` (compact) когда заперто — видно саму дверь «в 02:00 во вт».
> i18n en+ru (`adminPreviewAt/Now/Open/Closed`). npm check зелёный.
- **Цель:** настраивать окна не вслепую.
- **Модель данных:** нет (чисто UI-состояние).
- **Файлы:** админ-форма фигурки (`routes/admin/+page.svelte`) — контрол «предпросмотр в HH:MM»
  + локальный рендер `SealedDoor`/двери с подставленным `now`, без перевода системных часов и
  без записи в БД.
- **Переиспользовать:** `SealedDoor`, `isWindowOpen` с ручным `now`.
- **Приёмка:** двигаю предпросмотр-время → в форме вижу, открыта дверь или нет, для текущих настроек.
- **Зависит от:** ничего. Дешёвая, делать в одной волне с A.

---

## 2. Атмосфера — оживить дверь

### F. Свет из-под двери по мере приближения — `[x]` (2026-06-23)
> `minutesUntilOpen(w, now, 60)` в `showing-window.ts` (часы+дни+дата; null если открыто/далеко/
> нет часового окна; подтверждает реальный день/дату ближайшего открытия). В `SealedDoor` —
> `glow` 0→1, элемент `.door-glow` (тёплый радиальный градиент снизу/из центра, `mix-blend:screen`),
> opacity ∝ близости. Считается от `houseClock.nowDate` или проп `now` (админ-предпросмотр).
> Мерцание `door-flicker` только при `glow>0.75` и не при reduced-motion. Один элемент — перф ок.

### I. Ритуал открытия (дверь отпирается, не тает) — `[x]` (2026-06-23)
> Svelte `out:unlatch` на корне `SealedDoor`: гонит `@property --open` (1→0), створки разъезжаются
> (`translateX ±58%`), плафон/плашка растворяются; ассет-дверь — фейд. Срабатывает само, когда
> родительский `{#if doorClosed}` гаснет в минуту открытия (гость на странице). reduced-motion →
> быстрый фейд (220мс). Длительность 900мс, `cubicInOut`. npm check зелёный.

### (waves) Волна 2 закрыта — F + I.

### F (исходная карточка). Свет из-под двери — `[x]`
- **Цель:** за последний ~час до открытия дом «теплеет» — тёплое свечение в щель/скважину,
  усиливается плавно. БЕЗ цифр.
- **Модель данных:** нет. Чистая функция `minutesUntilOpen(w, now)` в `showing-window.ts`.
- **Файлы:** `showing-window.ts` (+helper), `SealedDoor.svelte` (градиент-glow, интенсивность ∝
  близости; анимируется ТОЛЬКО когда близко — не вся галерея, образец: `kh-breathe` в KeyholeVeil).
- **Приёмка:** дверь за 10 мин до открытия заметно теплее, чем за 3 часа; перф не падает на сетке дверей;
  `prefers-reduced-motion` → статичный мягкий свет без пульсации.
- **Зависит от:** база v1.

### G. Пыль/foxing как индикатор сна — `[ ]`
- **Цель:** далеко до открытия → дверь «пылится» сильнее; скоро → протёрта.
- **Модель данных:** нет (тот же `minutesUntilOpen`).
- **Файлы:** `SealedDoor.svelte` (слой пыли/foxing, opacity ∝ времени до открытия).
- **Переиспользовать:** визуальный язык foxing из `routes/figurines/[id]/+page.svelte`.
- **Приёмка:** видно «спящую» vs «вот-вот проснётся» дверь без чтения часов.
- **Зависит от:** F (общий helper). Делать вместе с F.

### H. Часовой скин всего архива — `[ ]`
- **Цель:** пергамент холоднее на рассвете, темнее в полночь — весь showcase дышит по реальному
  времени гостя; ночные экспонаты — часть живого дома.
- **Модель данных:** нет. Производная «оттенок по часу» из `house-clock.now`.
- **Файлы:** `routes/+layout.svelte` или `stores/theme.svelte.ts` — выставлять CSS-переменные тинта
  на корне; `app.css` — переменные парчи реагируют. Очень медленный ambient, аккуратно с reduced-motion.
- **Приёмка:** в полдень и в полночь архив ощутимо, но тонко различается; без рывков; не мешает читаемости.
- **Зависит от:** база v1. Самостоятельный крупный заход — отдельная волна.

---

## 3. Кульминация

### I. Ритуал открытия (дверь отпирается, не тает) — `[ ]`
- **Цель:** если гость на странице в минуту открытия — короткий латунный щелчок засова →
  створки расходятся / вертикальный wipe → работа. Один раз.
- **Модель данных:** нет. Засечь переход `doorClosed: true → false` пока компонент смонтирован.
- **Файлы:** `SealedDoor.svelte` (+ родители рендера) — анимация «отпирания»; вероятно вынести
  логику перехода в обёртку или в `$effect`, наблюдающий смену состояния от `house-clock`.
  `prefers-reduced-motion` → простой fade (как у таблички сейчас).
- **Приёмка:** сижу на закрытой карточке/детали, наступает минута открытия → проигрывается отпирание
  один раз, дальше обычная работа; при повторном закрытии назавтра — снова дверь; reduced-motion = fade.
- **Зависит от:** база v1. (Звук — отдельно, строго opt-in, в карту НЕ включён по решению владельца.)

---

## Порядок выполнения (волны)
- **Волна 1 — рычаги:** A (залы) + E (предпросмотр). Без них настройка не масштабируется.
- **Волна 2 — душа:** I (ритуал открытия) + F (свет под дверью). Превращает «работает» в «запоминается».
- **Волна 3 — мир дышит:** H (часовой скин архива).
- **Заполнение по нужде:** B (дни/даты), C (режим house), D (полудверь), G (пыль) — на общих helper'ах.

> Перед каждой задачей: проговорить план → подтверждение → код → обновить статус здесь.
