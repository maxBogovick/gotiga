# The desktop app that gave this directory its name is gone; only the backend
# server crate lives here now. The path is kept because start_all.sh,
# start_server.sh and the deployment docs point at src-tauri/server.
/server/target
/server/.idea
/server/uploads
/server/test**
/server/Cargo.lock
